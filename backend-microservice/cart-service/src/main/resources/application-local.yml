spring:
  config:
    import: classpath:application-common-local.yml
  datasource:
    url: ${CART_DATASOURCE_URL:jdbc:mysql://localhost:3307/ecommerce_cart?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
