spring:
  config:
    import: classpath:application-common-local.yml
  datasource:
    url: ${PAYOUT_DATASOURCE_URL:jdbc:mysql://localhost:3307/ecommerce_payout?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
