spring:
  config:
    import: classpath:application-common-local.yml
  datasource:
    url: ${USER_DATASOURCE_URL:jdbc:mysql://localhost:3307/ecommerce_user?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
